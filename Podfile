# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Mobile iOS Interview' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Mobile iOS Interview
  
  pod 'Reqres'

  target 'Mobile iOS InterviewTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
